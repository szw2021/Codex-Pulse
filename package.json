{
  "name": "codex-pulse",
  "productName": "Codex Pulse",
  "version": "0.2.11",
  "description": "A lightweight Tauri menu bar monitor for local and remote Codex CLI sessions.",
  "scripts": {
    "start": "tauri dev",
    "dev": "tauri dev",
    "build": "tauri build",
    "package": "tauri build --bundles app",
    "make": "TAURI_BUNDLER_DMG_IGNORE_CI=true CI=true tauri build --bundles app,dmg",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml && node --test Tests/notch-state.test.cjs && python3 -m unittest discover -s Tests",
    "check:lines": ".github/scripts/check-file-lines.sh",
    "check": "cargo check --manifest-path src-tauri/Cargo.toml && node --check src/renderer/theme-bootstrap.js && node --check src/renderer/tauri-bridge.js && node --check src/renderer/interactions.js && node --check src/renderer/app.js && node --check src/renderer/notch-state.js && node --check src/renderer/notch-status.js"
  },
  "engines": {
    "node": ">=22"
  },
  "author": "Codex Pulse contributors",
  "license": "MIT",
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
